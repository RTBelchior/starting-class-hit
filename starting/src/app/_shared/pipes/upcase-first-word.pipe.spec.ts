import { UpcaseFirstWordPipe } from './upcase-first-word.pipe';

describe('UpcaseFirstWordPipe', () => {
  it('create an instance', () => {
    const pipe = new UpcaseFirstWordPipe();
    expect(pipe).toBeTruthy();
  });
});
